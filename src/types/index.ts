export interface ModalContent {
    title: string;
    description: string;
    image?: string;
    yearRange?: string;
  }
  
  export interface InfoZoneData {
    position: [number, number, number];
    message: string;
  }